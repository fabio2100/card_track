* Add name or description to table payments. 
* Add name or desciption to the endpoint and the view for payments
* Add installments => check if it is better to make the installments via database(sql), or multiple calls to the same endpoint, in the form add monto total or monto de couta 